package ar.edu.ottokrause.psr;

public class IntegerArrayList<E> {

    int[] aOrigen = new int[10];
    int[] aDestino = new int[20];

    public IntegerArrayList() {
        //Recorre el vector aOrigin y copia el contenido del mismo en aDestino
        for (int i = 0; i < aOrigen.length; ++i) {
            for (int j = 0; j < aDestino.length; ++j) {
                aDestino[i] = aOrigen[i];
            }
        }
    }
    
    

}
